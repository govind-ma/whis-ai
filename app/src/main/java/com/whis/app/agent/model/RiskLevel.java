package com.whis.app.agent.model;

/**
 * Risk level classification for user messages and fraud scenarios (AI_AGENT_PLAN.md Section 4.2).
 */
public enum RiskLevel {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
